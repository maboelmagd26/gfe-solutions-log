# Accordion

**Status:** 🚧 In progress (static markup only, no expand/collapse state yet)

## TODO

- Expand/collapse state per section
- Keyboard support (section started in the old notes but never filled in —
  look up the WAI-ARIA accordion pattern: Enter/Space to toggle, arrow keys
  between headers)
- Tests
